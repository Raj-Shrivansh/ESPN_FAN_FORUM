<div class="card">
  <h5 class="card-header">Featured</h5>
  <div class="card-body">
    <h2 class="card-title"><%= @message.title %></h2>
    <p class="card-text">Posted<%=time_ago_in_words(@message.created_at)%> ago</p>
    <p class="card-text fs-5"><%= @message.desc %></p>
    <p class="d-inline-flex gap-1">
      <% if user_signed_in?  %>
      <% if current_user.id == @message.user_id%>
      <%= link_to 'Edit', edit_message_path(@message), class: "btn btn-secondary" %>
      <%= link_to 'Delete',@message,data: {turbo_method: 'delete', turbo_confirm: "Are you sure"},class:"btn btn-danger"%>
      <% end%>
      <% end%>
  <% if @message.comments.present? %>
      <h3>Comments :</h3>
      <%= render @message.comments %>
  <% end %>
  <h3>Join Discussion</h3>
  <%= render 'comments/form' %> 
</div>
</div>
<%= link_to 'Back',root_path,class:"btn btn-info btn-lg mt-3"%>